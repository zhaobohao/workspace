server:
  port: 80

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
    loadbalancer:
      retry:
        enabled: true

# 聚合文档配置
springclouddev:
  document:
    resources:
      - name: 授权模块
        location: /springcloud-auth
      - name: 工作台模块
        location: /springcloud-desk
      - name: 系统模块
        location: /springcloud-system



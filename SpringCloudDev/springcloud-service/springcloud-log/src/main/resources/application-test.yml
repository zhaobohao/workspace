#数据源配置
server:
  port: 8103

spring:
  datasource:
    url: ${springclouddev.datasource.test.url}
    username: ${springclouddev.datasource.test.username}
    password: ${springclouddev.datasource.test.password}
